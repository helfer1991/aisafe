#!/bin/sh
set -e

# Format only the Java files being committed, then re-stage them.
FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.java$' || true)
[ -z "$FILES" ] && exit 0

./mvnw -q spotless:apply
echo "$FILES" | xargs git add
